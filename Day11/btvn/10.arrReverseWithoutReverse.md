# Đảo ngược mảng không dùng reverse

- Cho một mảng các số nguyên, hãy viết hàm `reverseArr(arr)` đảo ngược mảng đó mà không sử dụng phương thức `reverse()`.
- Nếu mảng có các giá trị `null` hoặc `undefined`, hãy loại bỏ chúng trước khi đảo ngược.

```javascript
// Input:
const arrNumber = [1, 2, 3, 4, 5, 5, null, undefined, 6];
function reverseArr(arr) {
  // Xử lý và in ra kết quả
}

// Output:
const result = reverseArr(arrNumber);
console.log(result); // [6,5,4,3,2,1]
```
