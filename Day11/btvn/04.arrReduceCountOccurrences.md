# Đếm số lần xuất hiện của phần tử

Thực hiện viết hàm `countElement()` để đếm số lần xuất hiện của các phần tử trong mảng sử dụng `Array.reduce()` và trả về object chứa các phần tử và số lần xuất hiện tương ứng.

```javascript
// Input:
const fruits = [
  "apple",
  "banana",
  "kiwi",
  "kiwi",
  "banana",
  "orange",
  "apple",
  "kiwi",
];

function countElement(arr) {
  // Xử lý và in ra kết quả
}

// Output:
const result = countElement(fruits);
console.log(result); // { apple: 2, banana: 2, kiwi: 3, orange: 1 }
```
