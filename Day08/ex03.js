const users3 = [
  { id: 1, name: "Hoang", age: 30, isAdmin: true },
  { id: 2, name: "Huyen", age: 25, isAdmin: false },
  { id: 3, name: "Nam", age: 28, isAdmin: false },
];

for (let i = 0; i < users3.length; i++) {
  // console.log(users3[i]);
  if (users3[i].name === "Huyen") console.log(users3[i]);
}

/**
 * * Tìm bạn Huyền và in ra thông tin của bạn.
 * ? Các bài toán phổ biến với mảng: Lọc, tìm kiếm, sắp xếp lại, phân loại, tách, .v.v.
 */

/**
 * ? CRUD
 * * Create
 * * Read
 * * Update
 * * Delete
 */
